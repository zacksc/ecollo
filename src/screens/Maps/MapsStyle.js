import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
        mapContainer:{
        width: 450,
        height: 450,
        bottom: 70,
    },
    cardContainer:{
        bottom: 170,
        alignItems: 'center',
    },
})